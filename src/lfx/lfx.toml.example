# EarthMind Service Configuration
#
# This file tells LFX to use EarthMind's full-featured service implementations
# instead of the minimal/noop services built into LFX.
#
# Usage:
#   1. Copy this file to your EARTHMIND_CONFIG_DIR as lfx.toml
#   2. Or add to pyproject.toml under [tool.lfx.services]
#
# The services will be automatically discovered and loaded when you run:
#   - earthmind run
#   - lfx serve (when run from earthmind project)

[services]
# Core Services - Full-featured EarthMind implementations
database_service = "earthmind.services.database.service:DatabaseService"
storage_service = "earthmind.services.storage.local:LocalStorageService"
cache_service = "earthmind.services.cache.service:ThreadingInMemoryCache"

# Authentication & Session Management
auth_service = "earthmind.services.auth.service:AuthService"
session_service = "earthmind.services.session.service:SessionService"

# Chat & Communication
chat_service = "earthmind.services.chat.service:ChatService"

# State & Data Management
state_service = "earthmind.services.state.service:StateService"
variable_service = "earthmind.services.variable.service:VariableService"

# Background Processing
task_service = "earthmind.services.task.service:TaskService"
job_queue_service = "earthmind.services.job_queue.service:JobQueueService"

# Marketplace & Components
store_service = "earthmind.services.store.service:StoreService"
shared_component_cache_service = "earthmind.services.shared_component_cache.service:SharedComponentCacheService"

# Observability
telemetry_service = "earthmind.services.telemetry.service:TelemetryService"
tracing_service = "earthmind.services.tracing.service:TracingService"

# Note: settings_service is NOT configurable - it always uses the built-in implementation
# Note: mcp_composer_service uses the lfx implementation by default
