# earthmind-push.yml
#
# PURPOSE
#   Deploy (upsert) all flows to a remote EarthMind instance whenever
#   flow files are merged to main.  Uses stable flow IDs so re-running
#   this workflow always converges to the same state.
#
# PREREQUISITES
#   pip install lfx earthmind-sdk
#
# GITHUB SETUP  (Settings → Environments → New environment → "production")
#   Variables  (plain text):
#     EARTHMIND_PROD_URL        e.g. https://earthmind.example.com
#     EARTHMIND_PROJECT_NAME    e.g. "Production Flows"   (optional)
#   Secrets    (masked):
#     EARTHMIND_PROD_API_KEY    your production API key
#
#   Using a GitHub Environment lets you require manual approval before
#   each production deploy (Settings → Environments → Required reviewers).
#
# USAGE
#   Copy to .github/workflows/earthmind-push.yml in your repo.
#   The workflow triggers automatically on merges to main that touch
#   flows/ and can also be triggered manually from the Actions tab.

name: Deploy EarthMind Flows

on:
  push:
    branches:
      - main
    paths:
      - "flows/**/*.json"
  # Manual trigger with optional dry-run flag
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (show what would be pushed without making changes)"
        type: boolean
        default: false

jobs:
  push:
    name: lfx push
    runs-on: ubuntu-latest
    timeout-minutes: 10

    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      # ------------------------------------------------------------------ #
      # Install lfx + the SDK it needs to push flows.                      #
      # ------------------------------------------------------------------ #
      - name: Install dependencies
        run: pip install lfx earthmind-sdk

      # ------------------------------------------------------------------ #
      # Write environments config at CI time.                              #
      # The URL is a plain variable; the API key stays in secrets and is   #
      # resolved by earthmind-sdk via the api_key_env pointer at runtime.  #
      # ------------------------------------------------------------------ #
      - name: Write environments config
        run: >-
          printf '[environments.production]\nurl = "%s"\napi_key_env = "EARTHMIND_PROD_API_KEY"\n'
          "$EARTHMIND_PROD_URL" > earthmind-environments.toml
        env:
          EARTHMIND_PROD_URL: ${{ vars.EARTHMIND_PROD_URL }}

      # ------------------------------------------------------------------ #
      # Push flows.  Add --project to group flows into a folder.           #
      # Remove --project / --project-id lines if you don't use projects.  #
      # ------------------------------------------------------------------ #
      - name: Push flows
        env:
          # Resolved from the GitHub variable; falls back to "Main" if unset
          EARTHMIND_PROJECT_NAME: ${{ vars.EARTHMIND_PROJECT_NAME || 'Main' }}
          EARTHMIND_ENVIRONMENTS_FILE: earthmind-environments.toml
          EARTHMIND_PROD_API_KEY: ${{ secrets.EARTHMIND_PROD_API_KEY }}
        run: |
          lfx push \
            --dir flows/ \
            --env production \
            --project "$EARTHMIND_PROJECT_NAME" \
            ${{ github.event.inputs.dry_run == 'true' && '--dry-run' || '' }}

      - name: Summary
        if: always()
        run: |
          echo "### Deploy summary" >> "$GITHUB_STEP_SUMMARY"
          echo "- Environment: **production**" >> "$GITHUB_STEP_SUMMARY"
          echo "- Triggered by: ${{ github.event_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Dry run: ${{ github.event.inputs.dry_run || 'false' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Commit: \`${{ github.sha }}\`" >> "$GITHUB_STEP_SUMMARY"
