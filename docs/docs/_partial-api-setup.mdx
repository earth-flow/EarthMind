## Prerequisites

Before using the API, you need:

* [Install and start Terraflow](/get-started-installation) with the developer API enabled

    The Workflows API endpoints require the `developer_api_enabled` setting to be enabled. If this setting is disabled, these endpoints will return a 404 Not Found error.

    To enable the developer API endpoint, do the following:
    1. In the Terraflow `.env` file, set the environment variable to `true`:
       ```
       TERRAFLOW_DEVELOPER_API_ENABLED=true
       ```
    2. Start your Terraflow server with the `.env` file enabled:
       ```
       uv run terraflow run --env-file .env
       ```

    For more information about configuring environment variables, see [Environment variables](/environment-variables).
* [Create a Terraflow API key](/api-keys-and-authentication)
* [Create a flow](/concepts-flows) that you want to execute
* [Get the flow ID](/concepts-publish#api-access) or endpoint name of the flow you want to execute

### Set environment variables

All code examples in this documentation assume you have set the following environment variables:

**Python:**
```python
import os

TERRAFLOW_SERVER_URL = os.getenv("TERRAFLOW_SERVER_URL")
TERRAFLOW_API_KEY = os.getenv("TERRAFLOW_API_KEY")
```

**TypeScript/JavaScript:**
```typescript
const TERRAFLOW_SERVER_URL = process.env.TERRAFLOW_SERVER_URL;
const TERRAFLOW_API_KEY = process.env.TERRAFLOW_API_KEY;
```

Set these environment variables before running the examples, or replace the variable references in the code examples with your actual Terraflow server URL and API key.

The default `TERRAFLOW_SERVER_URL` for a local Terraflow deployment is `http://localhost:7860`.
For remote deployments, the domain is set by your hosting service, such as `https://UUID.ngrok.app`.

### Authentication and headers

All Workflows API requests require authentication using a Terraflow API key. The API key is passed in the `x-api-key` header.

For more information, see [Create a Terraflow API key](/api-keys-and-authentication).

| Header | Description | Example |
|--------|-------------|---------|
| `Content-Type` | Specifies the JSON format. | `application/json` |
| `x-api-key` | Your Terraflow API key. | `sk-...` |
| `accept` | Optional. Specifies the response format. | `application/json` |